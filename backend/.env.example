# Copy this file to backend/.env and fill values before running the backend.

# RideWise public contact email (also used as recipient)
REACT_APP_RIDEWISE_EMAIL=ridewise.support@gmail.com

# SMTP credentials for sending email (Gmail SMTP recommended with app password)
SMTP_EMAIL=ridewise.support@gmail.com
SMTP_PASSWORD=your_smtp_app_password_here
